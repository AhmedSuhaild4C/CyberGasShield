import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // telemetry is disabled via environment variable NEXT_TELEMETRY_DISABLED=1
  // Add other configurations here if needed (e.g., reactStrictMode: true)
};

export default nextConfig;
